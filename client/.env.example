# Revive — Frontend Environment Configuration Example
# Copy this file to client/.env when customizing environment variables.

# API Base URL — Set this in production (e.g. on Vercel) to point to your deployed Render backend API endpoint
# Leave blank or set to /api in development to use Vite proxy
VITE_API_BASE_URL=http://localhost:5005/api
